
# 操作手册

## 一、项目目录结构及主要文件说明

```
甲方：Company 机器
乙方：Partner 机器
协调方：Coordinator 机器
company/
    clean.sh                # 甲方一键清理脚本
    common.py               # 甲方通用函数
    company_share.csv       # 甲方与乙方共享数据
    host_test.csv           # 甲方测试集
    host_train.csv          # 甲方训练集
    LR.py                   # 逻辑回归相关代码
    PSI.py                  # PSI指标计算代码
    spu.log                 # 日志文件
    startA.sh               # 甲方一键启动脚本（训练入口）
    start_training.sh       # 甲方训练脚本（环境变量传入三机地址）
    start_infer.sh          # 甲方推理脚本（环境变量传入三机地址）
    start_ray_only.sh       # 仅启动 Company Ray Head
    truerun.py              # 甲方真实运行脚本
    models/lr_company/      # 甲方模型存储目录
coordinator/
    start_coordinator.sh    # Coordinator 机器加入 Ray 集群脚本
partner/
    clean.sh                # 乙方一键清理脚本
    guest_test.csv          # 乙方测试集
    guest_train.csv         # 乙方训练集
    joinB.sh                # 乙方加入 Ray 集群脚本
    models/lr_partner/      # 乙方模型存储目录
```

## 二、主要命令行操作指令

### 1. Company 机器先启动 Ray Head
```bash
source ~/conda_envs/sf-env/bin/activate
cd /home/lvx_vitae/AnonymVFL/company
bash clean.sh
export RAY_HEAD_HOST=192.168.10.11
export RAY_PORT=20001
bash start_ray_only.sh
```

### 2. Partner 机器加入 Ray 集群
```bash
source ~/conda_envs/sf-env/bin/activate
cd /home/lvx_vitae/AnonymVFL/partner
bash clean.sh
export RAY_HEAD_ADDR=192.168.10.11:20001
bash joinB.sh
```

### 3. Coordinator 机器加入 Ray 集群
```bash
source ~/conda_envs/sf-env/bin/activate
cd /home/lvx_vitae/AnonymVFL/coordinator
export RAY_HEAD_ADDR=192.168.10.11:20001
export COORDINATOR_HOST=192.168.10.13
bash start_coordinator.sh
```

### 4. Company 机器发起训练
```bash
source ~/conda_envs/sf-env/bin/activate
cd /home/lvx_vitae/AnonymVFL/company

export RAY_HEAD_ADDR=192.168.10.11:20001
export COMPANY_SPU_ADDR=192.168.10.11:11001
export PARTNER_SPU_ADDR=192.168.10.12:11002
export COORDINATOR_SPU_ADDR=192.168.10.13:11003

bash start_training.sh
```

### 5. Company 机器发起推理
```bash
source ~/conda_envs/sf-env/bin/activate
cd /home/lvx_vitae/AnonymVFL/company

export RAY_HEAD_ADDR=192.168.10.11:20001
export COMPANY_SPU_ADDR=192.168.10.11:11001
export PARTNER_SPU_ADDR=192.168.10.12:11002
export COORDINATOR_SPU_ADDR=192.168.10.13:11003

bash start_infer.sh SSLR
```

### 6. 三机部署约定

- Company: `192.168.10.11`
- Partner: `192.168.10.12`
- Coordinator: `192.168.10.13`

务必保证 Coordinator 与 Company 不在同一台机器。

建议启动顺序：Company Ray Head -> Partner -> Coordinator -> Company 训练/推理。

## 三、测试数据文件说明
- 数据文件：
  - company/host_train.csv
  - company/host_test.csv
  - partner/guest_train.csv
  - partner/guest_test.csv
