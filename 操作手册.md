
# 操作手册

## 一、项目目录结构及主要文件说明

```
甲方：服务器
乙方：客户端
company/
    clean.sh                # 甲方一键清理脚本
    common.py               # 甲方通用函数
    company_share.csv       # 甲方与乙方共享数据
    host_test.csv           # 甲方测试集
    host_train.csv          # 甲方训练集
    LR.py                   # 逻辑回归相关代码
    PSI.py                  # PSI指标计算代码
    SharedVariable.py       # 共享变量定义
    spu.log                 # 日志文件
    startA.sh               # 甲方一键启动脚本
    truerun.py              # 甲方真实运行脚本
    models/lr_company/      # 甲方模型存储目录
partner/
    clean.sh                # 乙方一键清理脚本
    guest_test.csv          # 乙方测试集
    guest_train.csv         # 乙方训练集
    joinB.sh                # 乙方一键启动脚本
    models/lr_partner/      # 乙方模型存储目录
```

## 二、主要命令行操作指令

### 1. 甲方启动(先启动)
```bash
source ~/conda_envs/sf-env/bin/activate
cd ~/moblie_project3/company
bash clean.sh
bash startA.sh
```

### 2. 乙方启动
```bash
source ~/conda_envs/sf-env/bin/activate
cd ~/moblie_project3/partner
bash clean.sh
bash joinB.sh
```

## 三、测试数据文件说明
- 数据文件：
  - company/host_train.csv
  - company/host_test.csv
  - partner/guest_train.csv
  - partner/guest_test.csv
