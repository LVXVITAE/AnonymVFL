# 轻量化底座数据接口设计

## 1. 背景

本系统当前训练和推理数据通过本地 CSV 路径直接读取，例如：

- `company/host_train.csv`
- `company/host_test.csv`
- `partner/guest_train.csv`
- `partner/guest_test.csv`

接入轻量化底座后，组件不再关心原始数据来源。底座会在部署或任务启动前，将组件所需数据统一落盘到 NFS 指定目录，并按 `数据资产编号.csv` 命名。组件通过底座接口获取订单资产、资产元数据和结果上报能力，通过环境变量获取 NFS 挂载根目录，最终读取：

```text
${NFS_MOUNT_PATH}/${metano}.csv
```

测试阶段使用本地 CSV 文件模拟 NFS 数据，读取规则保持一致。

## 2. 环境变量

| 变量名 | 说明 | 默认值 |
| --- | --- | --- |
| `BASE_INVOKE_URL` | 轻量化底座 MPI 调用地址 | 空 |
| `DATA_MODE` | 数据读取模式，支持 `local`、`nfs` | `local` |
| `NFS_MOUNT_PATH` | 生产环境 NFS 挂载目录 | `/data/nfs` |
| `LOCAL_ASSET_DIR` | 测试环境本地资产 CSV 目录 | `web_ui/mock_assets` |
| `MOCK_ASSETS_FILE` | 测试环境资产清单 | `web_ui/mock_assets.json` |
| `ORDER_CODE` | 当前任务订单号，可由底座部署时注入 | 空 |

## 3. 底座 MPI 对接

组件通过统一客户端调用底座地址 `/janus/invoke/v1`，请求体格式如下：

```json
{
  "method": "paas.engine.get.order.assets",
  "content": {
    "param": {}
  }
}
```

### 3.1 查询订单数据资产列表

- MPI：`paas.engine.get.order.assets`
- 组件接口：`GET /api/base/order-assets?orderCode=ORD202603050001`

返回订单下所有本方和合作方数据资产。组件使用 `assetSide` 区分：

- `LOCAL`：本方资产
- `PARTNER`：合作方资产

### 3.2 获取本方数据元信息

- MPI：`paas.metaset.detail`
- 组件接口：`GET /api/base/assets/{metano}?side=LOCAL`

用于查询本方单条数据资产的字段、行数、资产名称等元数据。

### 3.3 获取外部数据元信息

- MPI：`paas.metaset.partner.detail`
- 组件接口：`GET /api/base/assets/{metano}?side=PARTNER`

用于查询合作方单条数据资产的字段、行数、来源机构等元数据。

### 3.4 获取 CSV 读取路径

- 组件接口：`GET /api/base/assets/{metano}/path`

返回组件可直接读取的 CSV 路径：

```json
{
  "status": "success",
  "metano": "META_10001",
  "path": "/data/nfs/META_10001.csv",
  "exists": true
}
```

路径生成规则只允许 `metano + ".csv"`，不接收任意文件名，避免路径穿越。

### 3.5 上报计算结果

- MPI：`paas.engine.order.upload`
- 组件接口：`POST /api/base/results/report`

请求示例：

```json
{
  "orderCode": "ORD202603050001",
  "orderType": "OFFLINE_TASK",
  "extra": {},
  "output": {
    "resultPath": "/data/results/output.csv"
  }
}
```

组件接口会转换为底座规范请求体并调用 MPI。`DATA_MODE=local` 时返回 mock 成功，便于本地联调。

## 4. 训练数据选择约定

为兼容现有前端和训练脚本，`/api/start_training` 同时支持两种方式：

1. 直接传 CSV 路径：

```json
{
  "company_train_dataset": "/app/company/host_train.csv"
}
```

2. 传数据资产编号：

```json
{
  "company_train_metano": "META_COMPANY_TRAIN",
  "company_val_metano": "META_COMPANY_TEST",
  "partner_train_metano": "META_PARTNER_TRAIN",
  "partner_val_metano": "META_PARTNER_TEST"
}
```

也可以在原数据集字段中传 `asset://`：

```json
{
  "company_train_dataset": "asset://META_COMPANY_TRAIN"
}
```

后端会统一解析为 CSV 路径，再注入训练命令。

## 5. 本地测试模拟

本地测试阶段通过 `web_ui/mock_assets.json` 配置订单和资产清单，通过 `web_ui/mock_assets/*.csv` 模拟底座已落盘的数据。推荐将现有样例数据复制或挂载为：

```text
web_ui/mock_assets/META_COMPANY_TRAIN.csv
web_ui/mock_assets/META_COMPANY_TEST.csv
web_ui/mock_assets/META_PARTNER_TRAIN.csv
web_ui/mock_assets/META_PARTNER_TEST.csv
```

这样本地和生产都保持相同的 `metano -> {metano}.csv` 映射。

## 6. 模块职责

| 模块 | 职责 |
| --- | --- |
| `web_ui/base_client.py` | 封装底座 MPI 调用、本地 mock 返回、结果上报 |
| `web_ui/data_asset_service.py` | 解析 `metano` 到 CSV 路径，校验文件，生成本地元数据 |
| `web_ui/app.py` | 暴露组件内部 REST 接口，并在训练启动时解析数据资产路径 |

